# 家访材料管理系统 - 独立部署指南

## 系统简介

本系统是一个完全独立的家访材料收集管理平台，不依赖飞书/钉钉等任何第三方平台，任何人打开链接即可使用系统自带的账号密码登录。

### 核心功能
- 五级权限体系：管理员、校级领导、级长、年级主任、班主任
- 班主任只能管理本班家访材料，互不可见
- 管理员/校级领导可查看全校数据
- 级长/年级主任可查看本年级数据
- 文件上传、下载、在线查看
- 按月份自动汇总三个年级的家访数据
- 三大类别：常规家访、重点关爱学生家访、其他家访
- 四种家访形式：实地家访、到校家访、电话家访、微信家访
- 一键导入班主任名单和学生名单
- 批量下载附件

---

## 部署方案对比

| 方案 | 费用 | 难度 | 国内访问速度 | 推荐度 |
|------|------|------|-------------|--------|
| Render（推荐） | 免费 | 简单 | 一般 | ⭐⭐⭐⭐⭐ |
| 腾讯云 CloudBase | 免费额度 | 中等 | 快 | ⭐⭐⭐⭐ |
| 阿里云函数计算 | 免费额度 | 中等 | 快 | ⭐⭐⭐⭐ |
| 自己的服务器 | 自定 | 需技术 | 快 | ⭐⭐⭐ |

**推荐使用 Render**：完全免费、无需实名认证、部署最简单。

---

## 方案一：Render 一键部署（推荐）

### 准备工作

1. **注册 GitHub 账号**（免费）：https://github.com/signup
2. **注册 Render 账号**（免费）：https://dashboard.render.com/register
   - 可以用 GitHub 账号直接登录

### 第一步：上传代码到 GitHub

1. 登录 GitHub，点击右上角 `+` → `New repository`
2. 仓库名称填 `home-visit-system`，选择 `Public`，点击 `Create repository`
3. 下载本代码包并解压
4. 将解压后的所有文件上传到刚创建的 GitHub 仓库
   - 可以在仓库页面点击 `Add file` → `Upload files`，拖拽所有文件上传
   - 上传后点击 `Commit changes`

### 第二步：在 Render 上创建数据库

1. 登录 Render，点击顶部 `New +` → `PostgreSQL`
2. 填写：
   - **Name**: `home-visit-db`
   - **Database**: `home_visit`
   - **User**: `home_visit_user`
   - **Region**: 选 `Singapore`（新加坡，离国内最近）
   - **Plan**: 选 `Free`
3. 点击 `Create Database`
4. 等待数据库创建完成（约 1-2 分钟）
5. 创建完成后，在数据库详情页找到 `Connections` 区域，复制 `External Database URL`，备用

### 第三步：初始化数据库

1. 下载并安装 pgAdmin（免费 PostgreSQL 管理工具）：https://www.pgadmin.org/download/
2. 打开 pgAdmin，右键 `Servers` → `Register` → `Server`
3. 填写连接信息（从 Render 数据库详情页的 `Connections` 区域获取）：
   - **Host name/address**: Render 上的 Host
   - **Port**: Render 上的 Port
   - **Maintenance database**: `home_visit`
   - **Username**: `home_visit_user`
   - **Password**: Render 上的 Password
4. 连接成功后，右键数据库 `home_visit` → `Query Tool`
5. 打开代码包中的 `sql/init.sql` 文件，复制全部内容
6. 粘贴到 Query Tool 中，点击执行按钮（▶️）
7. 看到 `Query returned successfully` 即初始化完成

### 第四步：在 Render 上部署应用

1. 回到 Render，点击顶部 `New +` → `Web Service`
2. 选择 `Build and deploy from a Git repository`
3. 连接你的 GitHub 账号，选择刚才创建的 `home-visit-system` 仓库
4. 填写配置：
   - **Name**: `home-visit-system`（这个名字会成为访问链接的一部分）
   - **Region**: `Singapore`
   - **Branch**: `main`
   - **Runtime**: `Docker`
   - **Plan**: `Free`
5. 点击 `Advanced`，添加环境变量：
   - 点击 `Add Environment Variable`
   - **Key**: `DATABASE_URL`
   - **Value**: 粘贴第二步复制的 `External Database URL`
   - 再添加一个：
     - **Key**: `NODE_ENV`
     - **Value**: `production`
6. 点击 `Create Web Service`
7. 等待部署完成（首次部署约 5-10 分钟）
8. 部署成功后，页面顶部会显示一个链接，形如：`https://home-visit-system.onrender.com`

### 第五步：登录系统

1. 打开部署好的链接
2. 使用默认管理员账号登录：
   - **账号**: `admin`
   - **密码**: `admin123`
3. 登录后请立即修改密码（在个人设置中）

### 注意事项

- Render 免费版的 Web Service 15 分钟无访问会自动休眠，首次打开可能需要等待 30 秒左右唤醒
- Render 免费版的 PostgreSQL 数据库 90 天后会过期，到期前 Render 会发邮件提醒，可以免费续期
- 上传的文件存在 Render 的磁盘上，免费版有 1GB 空间
- 如果需要更好的稳定性，可以升级到 Render 付费版（$7/月起）

---

## 方案二：本地运行（测试用）

如果想先在自己电脑上测试，可以按以下步骤运行：

### 环境要求
- Node.js 20 或以上：https://nodejs.org/
- PostgreSQL 12 或以上：https://www.postgresql.org/download/

### 步骤

1. **安装依赖**
   ```bash
   cd home-visit-standalone
   npm install
   ```

2. **创建数据库**
   - 打开 pgAdmin 或 psql
   - 创建数据库 `home_visit`
   - 执行 `sql/init.sql` 初始化表结构

3. **配置环境变量**
   - 复制 `.env.example` 为 `.env`
   - 修改 `DATABASE_URL` 为你的本地数据库连接字符串

4. **启动开发服务器**
   ```bash
   npm run dev
   ```
   - 后端运行在 http://localhost:3000
   - 前端运行在 http://localhost:5173

5. **生产构建和运行**
   ```bash
   npm run build
   npm start
   ```
   - 访问 http://localhost:3000

---

## 常见问题

### Q: 打开链接显示 "Application Not Found" 怎么办？
A: 部署还在进行中，请等待 5-10 分钟后刷新。

### Q: 登录提示 "无操作权限" 怎么办？
A: 请确认使用的是管理员账号 `admin`，或者联系管理员分配角色。

### Q: 文件上传失败怎么办？
A: 检查服务器磁盘空间是否充足，Render 免费版有 1GB 限制。

### Q: 国内访问太慢怎么办？
A: 可以考虑使用国内云服务（腾讯云 CloudBase、阿里云等），或者使用 CDN 加速。

### Q: 如何备份数据？
A: 在 Render 数据库详情页可以下载数据库备份，也可以用 pgAdmin 导出数据。

### Q: 如何修改管理员密码？
A: 登录后在个人设置页面修改，或者直接在数据库中修改 `head_teacher` 表的 `password` 字段。

---

## 技术支持

如遇到部署问题，可以：
1. 查看 Render 的部署日志（Web Service 详情页 → Logs）
2. 检查数据库连接是否正确
3. 确认环境变量配置无误

---

## 默认账号

| 角色 | 账号 | 密码 | 权限 |
|------|------|------|------|
| 管理员 | admin | admin123 | 全校所有数据 |

**首次登录后请立即修改密码！**
